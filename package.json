{
  "name": "whycode",
  "private": true,
  "version": "0.1.0",
  "description": "以软件开发为核心专长的通用桌面 AI Agent，支持多模型协商决策",
  "scripts": {
    "dev": "pnpm --filter @whycode/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@11.9.0"
}
