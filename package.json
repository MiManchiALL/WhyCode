{
  "name": "whycode",
  "private": true,
  "version": "0.1.0",
  "description": "桌面端多模型 AI 编程 Agent，核心特色为多 Agent 协商决策",
  "scripts": {
    "dev": "pnpm --filter @whycode/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.9.0"
}
