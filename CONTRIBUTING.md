# Contributing to WhyCode

WhyCode 仍处于 Alpha 阶段。欢迎缺陷修复、测试、文档和边界清晰的功能改进；较大的协议、权限、持久化或界面结构变更，请先通过 Issue 说明目标和设计边界。

## 开发原则

1. **先找后写**：新增函数或组件前先查找同类实现，优先复用或扩展现有职责。
2. **治因而非遮掩**：先复现并定位根因，不用外围特判、重复状态或补偿流程掩盖可直接修复的问题。
3. **只实现当前需求**：不增加没有真实调用方的抽象、配置、缓存、标志位或未来占位接口。
4. **同步删除失效实现**：不要保留注释掉的代码、`old`/`v2` 平行实现或开发期双路径。
5. **保持职责内聚**：约 300 行/文件、50 行/函数是审查触发线而非机械上限；只有拆分能提升可读性和可测试性时才拆。
6. **依赖保持克制**：新增依赖前确认现有能力无法合理解决，并优先使用维护成熟的基础库而不是复制通用基础设施。
7. **注释解释原因**：说明约束、取舍和不明显的边界，不逐行翻译代码，不提交调试输出。
8. **安全默认关闭**：未知模型能力、权限、路径、网络目标和恢复状态必须 fail closed，不根据名称或偶然成功进行推断。

## 架构边界

- `packages/core` 维护模型无关语义，不依赖 Electron 或 Renderer。
- Electron Main 负责文件系统、进程、网络、安全存储和运行时生命周期。
- Renderer 只负责交互与事件投影；纯界面改动不得顺带改变 Core、IPC、JSONL、会话 schema 或权限语义。
- CoreEvent / CoreCommand 必须保持 JSON-safe。新增事件时同步更新宿主适配、运行时重放、可见事件契约和相关测试。
- 当前仍在开发，不为未发布的旧开发会话增加迁移器、双格式解析或展示兜底；若需要正式数据迁移，应先单独设计和评审。

## 前端约束

- 复用现有 Radix 菜单、选择器、纸贴 tokens 和布局组件，不引入原生 `select` 或第二套弹窗体系。
- 保持中央时间线克制；装饰只用于明确分组，不覆盖正文、工具卡和关键状态。
- 检查窄窗口、长文本、键盘焦点、输入法组合、菜单清晰度和内容溢出。
- 避免实时滤镜、Canvas 逐帧效果、长期 `will-change` 或对正文容器整体变换。

## 提交前验证

根据改动范围运行定向测试，并在提交前完成：

```bash
pnpm test
pnpm typecheck
pnpm build
pnpm audit --prod
git diff --check
```

测试失败时请修复根因；如果存在明确的平台限制或外部依赖，应在提交说明中如实记录，不能把未执行的验证写成通过。

## 安全与隐私

- 不提交 API Key、令牌、Cookie、私钥、真实会话数据或本机配置。
- 示例路径、账号和服务地址必须使用无身份含义的占位值。
- 日志、截图和测试夹具在提交前必须脱敏。
- 如果密钥曾进入 Git 历史，即使当前文件已经删除，也应立即轮换并清理历史。
