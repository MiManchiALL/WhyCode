export const ANALYZE_IMAGE_TOOL_NAME = 'AnalyzeImage'

export const ANALYZE_IMAGE_PROMPT = `使用已配置的辅助识图模型读取当前对话里用户已上传的图片。仅当主模型本身不支持图片输入时提供本工具。

调用规则：
- 当回答依赖图片像素、图中文字、版式、颜色、位置、数量或多图差异时，先调用本工具再回答；不要凭文件名或上下文猜测。
- attachmentIds 只能使用当前对话消息中显示的“附件 ID”，并按希望辅助模型查看的顺序填写；不要编造 ID。
- question 必须由你结合完整对话意图重新表述为独立、明确的视觉问题，不能机械复制用户最新一句。
- 要解析省略、代词和跨轮指代。例如用户先问“蓝圈里是什么”，下一条只上传图片时，question 应明确要求重点分析图片蓝圈区域。
- 保留用户要求的关注区域、颜色、对象、比较关系、OCR 字段和输出格式；多图比较时说明图片顺序与比较维度。
- 只传与视觉观察有关的必要上下文，不要把项目机密、完整对话或无关任务塞进 question。
- 一次结果不够时，可以对同一附件用更具体的问题再次调用。`
