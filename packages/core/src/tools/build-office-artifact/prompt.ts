export const BUILD_OFFICE_ARTIFACT_TOOL_NAME = 'BuildOfficeArtifact'

export const BUILD_OFFICE_ARTIFACT_PROMPT = `${BUILD_OFFICE_ARTIFACT_TOOL_NAME} 在一次性、限时限内存且无 Node 环境权限的 SES 构建隔离区执行 JavaScript 函数，并原子生成 DOCX、PPTX 或 XLSX。先用 WriteFile 创建 .js 构建脚本；脚本必须是函数表达式，形如 async ({ docx, PptxGenJS, ExcelJS, JSZip, fastXml, OfficeTemplate, assets, report }) => artifact。不要 import、require、访问文件系统、启动进程或自行写输出；依赖与资源由工具注入。DOCX 返回 docx.Document 或 OOXML bytes，PPTX 返回 PptxGenJS 实例或 OOXML bytes，XLSX 返回 ExcelJS.Workbook 或 OOXML bytes。assets 通过 key 访问，包含 name、extension、bytes、base64 和 dataUri。mode=create 只用于没有指定格式控制文件的从零创建；只要用户指定现有 Office 文件作为模板、母版、参考版式或编辑原件，就必须用 mode=template，把原件作为 asset 并用 templateAssetKey 指向它。template 模式会在执行前验证原件，发布前证明共享样式/母版/版式/媒体和格式锚点仍源自模板；用空白 docx.Document 或 PptxGenJS 仅模仿颜色不算模板继承。DOCX 模板接口固定为 OfficeTemplate.docx({ template: assets.template.bytes, textEdits, rangeEdits })；PPTX 模板接口固定为 OfficeTemplate.pptx({ template: assets.template.bytes, slides })；两者都直接返回 OOXML bytes，不接受把 bytes 直接传给函数，也不返回带 slide/replaceShapeText 方法的可变模板对象。PPTX 模板调用前必须同时读取对应 Skill 的 references/builder-api.md 与 references/template-following.md，先按源文件 overview 盘点叙事角色、构图轮廓、视觉密度和媒体槽，再把输出页映射到同类 sourceSlide；不能把结束/章节页借作封面或正文，也不能反复选择少数纯文字页来规避内容视觉。PPTX 模板计划必须为每个输出页的每个 slide-level 媒体 shape 明确提供且只提供一个 keep、image 或 delete 动作；image/delete 声明 mediaRole 与 reason，keep 仅限 brand/decoration，content 媒体必须用新主题 asset 替换或删除。输入资料含相关图表、界面或图片时应先准备为显式 asset，不能全部改写成文字；“模板继承”只证明 OOXML 结构血缘，不代表整套视觉保真。其它格式调用前必须读取对应 Skill 的 references/builder-api.md；接口或脚本报错时先修正同一个 builder，不要用相同参数盲目重试或创建一组调试 builder。XLSX 用 ExcelJS load 模板后局部修改。XLSX 含公式时，发布前会用 Microsoft Excel 或 LibreOffice 实际重算、保存并重新检查；没有重算引擎、仍有未保存计算值或公式错误时构建失败。生成后必须用 InspectOffice 做内容、对象、样式和校验复核；视觉模型还必须先用 RenderOffice overview 检查整套节奏，再用 pages 复核全部最终页。`
