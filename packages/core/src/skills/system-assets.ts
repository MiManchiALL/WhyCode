import { OFFICE_SYSTEM_SKILL_FILES } from './system-office-assets.ts'

export interface EmbeddedSystemSkillFile {
  relativePath: string
  content: string
}

export const EMBEDDED_SYSTEM_SKILL_FILES: readonly EmbeddedSystemSkillFile[] = [
  {
    relativePath: 'code-review/SKILL.md',
    content: document(
      '---',
      'name: code-review',
      'description: 只读审查未提交变更、分支差异、提交或自定义目标；报告全部由本次改动引入、能从代码与调用路径证实且作者会修复的可行动缺陷。',
      '---',
      '',
      '# Code Review',
      '',
      '审查用户指定的目标。未指定时，审查当前工作区相对 HEAD 的 staged、unstaged 和相关 untracked 变更；目标不可解析或没有差异时说明事实，不猜测其他目标。',
      '只读执行，不修改文件、创建提交、推送分支或发布远程评论。',
      '',
      '## 审查流程',
      '',
      '1. 读取适用的项目约束和自定义要求，检查完整 diff 及必要的相邻代码，明确改动意图和必须保持的行为。',
      '2. 追踪受影响的生产者、消费者、替代入口、权限执行点和资源释放路径，重点检查正确性、安全、并发、生命周期以及错误与取消传播。',
      '3. 检查相关调用点和测试。绿色测试、覆盖率或快照不是充分证据；相关时验证真实入口和外部可观察行为。公共契约变化时，检查类型、Schema、JSDoc 和文档是否同步。',
      '4. 审完整个目标，不在首个发现后停止。',
      '',
      '审查分支或 PR 时，确认实际 base 和 head，并以 `git merge-base` 计算会合并的差异；无法可靠确定基线时说明事实，不猜测。',
      '',
      '只报告同时满足以下条件的问题：',
      '',
      '- 问题由本次审查的改动引入。',
      '- 对正确性、安全、性能或具体维护风险有实质影响。',
      '- 能证明触发场景和受影响路径。',
      '- 问题离散、可行动，且作者得知后大概率会修复。',
      '',
      '不报告推测、既有问题、刻意且一致的行为变化或纯风格偏好。',
      '',
      '## 输出结果',
      '',
      '发现优先，按 P0 到 P3 排序。每项使用：',
      '',
      '`[P1] 命令式标题 — path/to/file.ts:line`',
      '',
      '随后用一小段说明触发场景、影响和错误原因。引用范围保持最小并与本次 diff 重叠。',
      '',
      '- `P0`：普遍发生的发布阻断或严重故障。',
      '- `P1`：应当优先修复的紧急缺陷。',
      '- `P2`：应当修复的普通缺陷。',
      '- `P3`：影响较低但仍值得修复的实质问题。',
      '',
      '没有合格发现时明确写“未发现需要修复的问题”，不为填充结果制造问题。最后只补充实质测试缺口或剩余风险；没有则省略。',
    ),
  },
  {
    relativePath: 'debug/SKILL.md',
    content: document(
      '---',
      'name: debug',
      'description: 诊断错误、失败测试、异常行为或性能退化；先最小复现并定位首个错误状态，再做最小根因修复与回归验证。',
      '---',
      '',
      '# Debug',
      '',
      '从可观察事实开始修正产生问题的逻辑，不用重试、忽略、放宽校验、兜底或厂商特判遮蔽根因。',
      '',
      '## 工作流',
      '',
      '1. 固定用户观察到的现象、输入、环境和期望行为；先运行最小复现。',
      '2. 先窄后宽地读取相关日志、错误、状态和代码路径，区分已观察事实与推断，避免把无关大段输出塞入上下文。',
      '3. 每次验证一个可证伪假设，沿数据流或控制流追到首个错误状态产生点，并用证据排除相邻解释。',
      '4. 尚未复现或确认根因时不凭猜测改代码；说明证据缺口和下一项最能区分假设的检查。',
      '5. 在既有职责边界内实施最小修复，不顺手重构。可稳定自动化时，先补能在修复前失败的回归测试。',
      '6. 重跑原复现、针对性测试、受影响包检查和必要的相邻失败路径，确认错误消失且预期行为保留。',
      '',
      '## 结果要求',
      '',
      '报告根因、证据链、改动点、原复现与修复后的精确结果、执行过的验证及剩余风险。没有运行的检查不得写成已通过。',
    ),
  },
  {
    relativePath: 'simplify/SKILL.md',
    content: document(
      '---',
      'name: simplify',
      'description: 简化当前任务相关的代码；删除重复实现、冗余状态、泄漏抽象和不必要工作，同时保持已约定行为与接口不变。',
      '---',
      '',
      '# Simplify',
      '',
      '直接清理当前任务相关的代码。优先检查完整 diff；没有 diff 时，只检查本次任务已编辑、用户明确指定或已选定的简化候选。没有可确定范围时说明现状并停止。',
      '这是当前任务内、行为保持的实现清理，不扩展为全项目审计、架构提案或无关重构。若简化必须改变需求、公共行为或已约定接口，只说明原因，不实施。',
      '',
      '## 清理流程',
      '',
      '1. 读取项目约束和完整 diff，明确改动意图、公共契约与必须保持的行为。',
      '2. 搜索语言或运行时内置能力、仓库已有依赖、工具、类型和相邻模式，优先复用。只有项目策略允许、语义覆盖完整且确实减少净复杂度时，才新增依赖。',
      '3. 删除或合并代码前，确认生产调用点、动态注册、配置与序列化入口、字符串协议和公开契约；静态搜索没有结果不能单独证明代码未被使用。',
      '4. 清理重复或近似复制的实现、可派生却重复保存的状态、绕过根因的标志位、过度参数传递、泄漏的内部细节、重复工作和仅翻译代码表面行为的注释；保留解释隐藏约束与设计原因的注释。',
      '5. 保留跨越不可信输入或所有权转移边界所需的验证、复制和隔离。对异步与生命周期代码，只合并表达同一事实的机制；不要误删分别负责取消、回滚、终态仲裁、重入保护或完整释放的逻辑。',
      '6. 只做收益明确的修改，不为单次使用新增抽象，不做无关格式化。运行最贴近改动的测试和类型检查；相关时验证真实入口、外部行为和资源清理结果。',
      '',
      '最后简要列出实际清理项和验证结果。没有可靠简化时明确说明，不为制造改动而修改。',
    ),
  },
  {
    relativePath: 'find-simplifications/SKILL.md',
    content: document(
      '---',
      'name: find-simplifications',
      'description: 只读调查整个项目或指定范围内的高置信简化候选；仅在用户要求系统性审计时使用，当前改动的直接清理使用 simplify。',
      '---',
      '',
      '# Find Simplifications',
      '',
      '调查用户指定的范围。只有请求明确指向整个项目时才覆盖整个仓库；未明确范围时，只调查用户当前指向的模块或变更，仍无法确定时说明并停止。',
      '这是只读调查与提案任务，不修改代码。候选的实施交给 `simplify`，并按用户选定的范围分批完成。',
      '',
      '## 调查流程',
      '',
      '1. 读取适用的项目约束、架构说明和公共契约，明确主要入口、模块边界、生产代码范围和已有职责。',
      '2. 搜索语言与运行时内置能力、仓库已有依赖、工具和相邻模式，识别重复实现、镜像状态、无生产消费者的表面、手写替代、泄漏抽象和不必要的兼容层。',
      '3. 对每个候选检查生产消费者、动态注册、配置与序列化入口、字符串协议、公共契约、测试与文档，以及状态和资源的所有权与生命周期关系；静态搜索没有结果不能单独证明可以删除。',
      '4. 评估净收益，包括可删除表面、残余胶水、行为损失、迁移成本、接口影响和验证成本。拒绝把产品取舍或有明确理由的复杂性伪装成清理。',
      '5. 完成约定范围的调查，不在首个候选后停止。候选过多时，只保留收益、证据强度和可实施性最高的一组。',
      '',
      '## 输出结果',
      '',
      '每个候选说明：',
      '',
      '- 建议删除、合并、复用或降级的具体表面；',
      '- 生产消费者、动态入口和契约证据；',
      '- 预期净收益；',
      '- 可能失去的行为及风险；',
      '- 实施范围和必要验证。',
      '',
      '按收益、风险和证据强度排序，区分：',
      '',
      '- 可直接交给 `simplify` 的低风险清理；',
      '- 需要产品或接口决策的设计变化。',
      '',
      '仅在某个看似可删的复杂性很容易被误判时，简要说明保留理由。没有可靠候选时明确说明，不为形成报告而制造建议。',
    ),
  },
  {
    relativePath: 'skill-creator/SKILL.md',
    content: document(
      '---',
      'name: skill-creator',
      'description: 创建或更新 WhyCode Agent Skill；用于把重复工作流、专业知识或工具用法沉淀到项目级或用户级 Skill，并验证触发边界。',
      '---',
      '',
      '# Skill Creator',
      '',
      '动手前，使用当前 Skill 的精确 id 读取 `references/checklist.md`。更新现有 Skill 时，先读取整个包和本次任务中已经验证的工作方式与用户纠正，保留仍有用的内容并删除过时内容。',
      '',
      '## 设计',
      '',
      '1. 先确认任务重复、稳定且值得复用；一次性说明留在当前任务，不为形式创建 Skill。只询问会阻断设计的未知项。',
      '2. 提炼触发与排除场景、输入、输出、必要步骤、可观察成功条件和停止条件；脆弱操作才写精确顺序，开放任务保留判断空间。',
      '3. 仓库专用流程放在从 Git 根到当前目录合适层级的 `.agents/skills/<name>/`；跨项目个人流程放在 `$HOME/.whycode/skills/<name>/`。不写入 WhyCode 托管的 `.whycode/skills/.system`。',
      '4. 名称使用不超过 64 字符的小写 kebab-case，并与目录名完全一致。',
      '5. `SKILL.md` frontmatter 只使用 WhyCode 支持的 `name`、`description`，不复制其它宿主扩展字段。description 前置说明做什么、何时用和关键边界。',
      '6. 默认只写指令；仅在需要确定性或重复执行时增加 `scripts/`，长且按需的资料才放 `references/`。正文说明何时使用资源，不增加重复 README。',
      '7. 使用命令式短句，只保留能避免错误的非显然约束。优先复用项目命令与事实源；外部或不可逆动作才设置人工确认，不写隐藏权限或未来占位。',
      '',
      '## 验证',
      '',
      '重新读取全部文件并按清单验收；实际运行新增或修改的脚本。用一个明确匹配的请求验证可发现性，再用一个相邻但不匹配的请求确认 description 不会过度触发。',
    ),
  },
  {
    relativePath: 'skill-creator/references/checklist.md',
    content: document(
      '# WhyCode Skill 检查清单',
      '',
      '- 包目录名称与 frontmatter `name` 完全相同。',
      '- `name` 仅含小写字母、数字和单连字符，长度不超过 64。',
      '- frontmatter 仅含 `name` 与 `description`；不复制其它宿主的扩展字段。',
      '- `description` 先说明做什么与何时使用，再给关键边界，长度不超过 1024 字符。',
      '- `SKILL.md` 是有效 UTF-8，大小不超过 256 KiB。',
      '- 用户级路径为 `$HOME/.whycode/skills/<name>/SKILL.md`；项目级路径位于 Git 根到当前目录某层的 `.agents/skills/<name>/SKILL.md`。',
      '- 资源使用包内相对路径，不含空段、`.`、`..`、绝对路径或符号链接。',
      '- 正文只做一个工作，使用命令式短句；输入、输出、脆弱步骤的成功/停止条件清楚，没有教程、重复事实或未来占位。',
      '- 大型资料只在按需读取时放入 `references/`；脚本只承载确定性或重复执行逻辑，正文说明何时使用，新增或修改后已实际运行。',
      '- Skill 只提供指令和资源，不声称获得新的文件、命令、网络或审批权限。',
      '- 更新现有包时已读取全部资源，保留有效内容并删除失效或无人引用的内容。',
      '- 用明确匹配请求和相邻但不匹配请求各验证一次触发边界。',
    ),
  },
  {
    relativePath: 'verify/SKILL.md',
    content: document(
      '---',
      'name: verify',
      'description: 验收当前实现、修复或重构；用于用户要求验证、回归检查或合并前确认时，以真实运行证据给出 PASS、FAIL 或 PARTIAL。',
      '---',
      '',
      '# Verify',
      '',
      '默认只读取和执行验证，不修改产品代码；可以创建必要的临时验证数据，但结束后必须清理。验证失败时先报告证据和责任边界，除非用户同时要求修复。',
      '',
      '## 步骤',
      '',
      '1. 从用户要求、变更和项目文档提取必须成立的行为，只列与风险相关的正常、边界、失败与恢复路径。',
      '2. 选择能直接观察结果的方法：缺陷重跑原复现，CLI 检查 stdout/stderr/退出码，服务检查真实请求与错误响应，UI 执行真实交互或渲染，重构核对公开行为不变。',
      '3. 优先复用项目测试和脚本，按“最小针对性 → 受影响包 → 项目规定的 build/typecheck/lint/test”扩展。测试通过只是辅助证据，不得替代任务要求的真实行为。',
      '4. 对非平凡改动至少选择一个相关的反例或对抗性检查，例如边界、幂等、并发或清理；不机械执行无关组合。',
      '5. 记录命令或操作、输入、关键原样输出和退出状态；需要比较时同时记录基线。清理临时进程与文件。',
      '6. 判定失败前排除错误预期、上下游已经处理、测试缺陷和环境限制；保留首个有诊断价值的错误。',
      '',
      '## 输出',
      '',
      '按“行为 / 方法 / 结果 / 证据”给出验收表，再给出 PASS、FAIL 或 PARTIAL。PARTIAL 必须列明未执行项与原因；只把实际观察到的结果写成结论。',
    ),
  },
  ...OFFICE_SYSTEM_SKILL_FILES,
]

function document(...lines: string[]): string {
  return `${lines.join('\n')}\n`
}
