import { OFFICE_SYSTEM_SKILL_FILES } from './system-office-assets.ts'

export interface EmbeddedSystemSkillFile {
  relativePath: string
  content: string
}

export const EMBEDDED_SYSTEM_SKILL_FILES: readonly EmbeddedSystemSkillFile[] = [
  {
    relativePath: 'code-review/SKILL.md',
    content: document(
      '---',
      'name: code-review',
      'description: 只读审查当前代码变更，优先报告可复现、可定位、值得修复的缺陷。',
      '---',
      '',
      '# Code Review',
      '',
      '对用户指定的 diff、提交或分支做只读审查。用户没有指定目标时，审查当前工作区相对 HEAD 的 staged、unstaged 与相关 untracked 代码；没有可审查变更时直接说明，不猜测其它目标。',
      '',
      '## 步骤',
      '',
      '1. 读取项目约束、完整变更和理解调用链所需的相邻代码。',
      '2. 检查受影响的调用点、测试、错误路径、权限边界、并发与资源生命周期。',
      '3. 只报告由本次变更引入，而且能说明触发条件和影响的离散问题。继续审完整个目标，不在首个发现后停止。',
      '4. 不修改文件，不创建提交，不推送，不把纯风格偏好、既有问题或无法验证的猜测写成缺陷。',
      '',
      '## 输出',
      '',
      '发现优先，按 P0 到 P3 排序。每项使用：',
      '',
      '`[P1] 命令式标题 — path/to/file.ts:line`',
      '',
      '随后用一小段说明具体场景、错误行为和影响，行号范围保持最小。若没有符合条件的发现，明确写“未发现需要修复的问题”，再补充尚未覆盖的实质测试风险。',
    ),
  },
  {
    relativePath: 'debug/SKILL.md',
    content: document(
      '---',
      'name: debug',
      'description: 复现故障、沿单一证据链定位根因，实施最小修复并补回归验证。',
      '---',
      '',
      '# Debug',
      '',
      '处理可复现的错误、异常行为、失败测试或性能退化。目标是修正产生问题的逻辑，而不是在外围增加重试、忽略、兜底或厂商特判。',
      '',
      '## 工作流',
      '',
      '1. 固定用户观察到的现象、输入、环境和期望行为；先运行最小复现。',
      '2. 收集日志、错误、状态和相关代码路径，明确区分已观察事实与推断。',
      '3. 沿一条数据流或控制流追到首个错误状态产生点，并用证据排除相邻假设。',
      '4. 在既有职责边界内实施最小根因修复；不要顺手做无关重构。',
      '5. 能稳定复现时先补失败回归，再运行针对性测试、受影响包检查和必要的更广验证。',
      '6. 重新执行原复现，确认错误行为消失且预期行为保留。',
      '',
      '## 结果要求',
      '',
      '报告根因、证据链、改动点、原复现与修复后的精确结果、执行过的验证及剩余风险。没有运行的检查不得写成已通过。',
    ),
  },
  {
    relativePath: 'simplify/SKILL.md',
    content: document(
      '---',
      'name: simplify',
      'description: 在保持行为不变的前提下清理当前代码变更中的重复、冗余状态和低质量实现。',
      '---',
      '',
      '# Simplify',
      '',
      '审查并直接清理当前任务相关的代码变更。若工作区没有相关变更，说明现状并停止，不任意重写项目。',
      '',
      '## 检查顺序',
      '',
      '1. 查看完整 diff 和项目约束，确定变更意图与必须保持的行为。',
      '2. 搜索已有工具、类型、组件和相邻模式，删除重复实现与手写替代品。',
      '3. 清理可派生却被重复保存的状态、仅为绕过根因的标志位、近似复制代码、过度参数传递、字符串化类型和泄漏的内部细节。',
      '4. 检查重复 I/O、无界集合、监听器泄漏、热路径阻塞、无变化却反复通知以及本可并发的独立工作。',
      '5. 删除翻译代码表面行为的注释和失效分支；保留解释隐藏约束与设计原因的注释。',
      '6. 只做能明确提高复用、可读性或效率且不改变需求的修改，然后运行最贴近改动的测试与类型检查。',
      '',
      '最后简要列出实际清理项和验证结果；发现建议不成立时跳过，不为制造改动而修改。',
    ),
  },
  {
    relativePath: 'skill-creator/SKILL.md',
    content: document(
      '---',
      'name: skill-creator',
      'description: 创建或改进符合 WhyCode Agent Skills 目录、元数据和渐进披露约束的 Skill。',
      '---',
      '',
      '# Skill Creator',
      '',
      '为重复、稳定且值得复用的工作流创建或更新 Skill。动手前，使用当前 Skill 的精确 id 读取 `references/checklist.md`，并按其中清单验证结果。',
      '',
      '## 设计',
      '',
      '1. 从用户请求中提炼触发场景、输入、按顺序执行的步骤、可验证产物和停止条件。',
      '2. 仓库专用流程放在从 Git 根到当前目录合适层级的 `.agents/skills/<name>/`；跨项目个人流程放在 `$HOME/.agents/skills/<name>/`。不写入 WhyCode 托管的 `.whycode/skills/.system`。',
      '3. 名称使用不超过 64 字符的小写 kebab-case，并与目录名完全一致。',
      '4. `SKILL.md` 只保留 `name`、`description` frontmatter 和完成任务所需的核心指令。description 要清楚说明何时使用。',
      '5. 长参考资料拆到 `references/`，可执行辅助放到 `scripts/`；正文明确说明何时读取或运行，避免复制同一事实。',
      '6. 指令使用命令式表达，优先复用项目已有命令与事实源，不写厂商专用会话状态、隐藏权限或未来占位。',
      '',
      '## 验证',
      '',
      '创建后重新读取全部文件，确认 UTF-8、frontmatter、名称/目录一致、相对资源路径和正文步骤完整。用一个明确匹配的请求验证可发现性，再用一个不匹配请求确认 description 不会过度触发。',
    ),
  },
  {
    relativePath: 'skill-creator/references/checklist.md',
    content: document(
      '# WhyCode Skill 检查清单',
      '',
      '- 包目录名称与 frontmatter `name` 完全相同。',
      '- `name` 仅含小写字母、数字和单连字符，长度不超过 64。',
      '- `description` 非空、说明适用场景，长度不超过 1024 字符。',
      '- `SKILL.md` 是有效 UTF-8，大小不超过 256 KiB。',
      '- 用户级路径为 `$HOME/.agents/skills/<name>/SKILL.md`；项目级路径位于 Git 根到当前目录某层的 `.agents/skills/<name>/SKILL.md`。',
      '- 资源使用包内相对路径，不含空段、`.`、`..`、绝对路径或符号链接。',
      '- 核心步骤在正文中自洽；大型参考资料按需放入 `references/`，没有重复事实源。',
      '- Skill 只提供指令和资源，不声称获得新的文件、命令、网络或审批权限。',
      '- 用匹配请求和不匹配请求各验证一次触发边界。',
    ),
  },
  {
    relativePath: 'verify/SKILL.md',
    content: document(
      '---',
      'name: verify',
      'description: 为当前改动建立行为验收矩阵，运行真实命令并给出可核对的通过或失败证据。',
      '---',
      '',
      '# Verify',
      '',
      '验证当前实现是否满足用户要求。默认只读取和执行验证，不修改产品代码；如果验证失败，先报告失败证据和最可能责任边界，除非用户同时要求修复。',
      '',
      '## 步骤',
      '',
      '1. 从用户要求、变更和项目文档提取必须成立的行为，列出正常、边界、失败与恢复路径。',
      '2. 优先复用项目已有测试和脚本；先运行最小针对性检查，再运行受影响包和项目要求的完整检查。',
      '3. UI、CLI 或服务行为需要真实交互时，启动实际目标并使用具体输入验证，不用静态代码阅读替代运行证据。',
      '4. 每项记录基线/当前命令或操作、输入、关键原样输出和退出状态；清理验证产生的临时进程与临时文件。',
      '5. 失败时保留首个有诊断价值的错误，区分产品缺陷、环境限制和测试本身问题。',
      '',
      '## 输出',
      '',
      '按“行为 / 方法 / 结果 / 证据”给出验收表，再总结已通过、未通过和未执行项。只把实际观察到的结果写成结论。',
    ),
  },
  ...OFFICE_SYSTEM_SKILL_FILES,
]

function document(...lines: string[]): string {
  return `${lines.join('\n')}\n`
}
